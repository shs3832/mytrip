
services:
  mytrip:
    # 현재 폴더를 Docker 빌드 컨텍스트로 사용한다.
    build:
      context: .
      dockerfile: Dockerfile
    # 내 컴퓨터의 2500번 포트를 컨테이너의 3000번 포트에 연결한다.
    # 브라우저에서는 http://localhost:2500 으로 접속하면 된다.
    ports:
      - "2500:3000"
    
